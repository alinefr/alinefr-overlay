diff -Naur rt3290sta.orig/Makefile rt3290sta/Makefile
--- rt3290sta.orig/Makefile	2015-09-09 18:33:56.554310269 -0300
+++ rt3290sta/Makefile	2015-09-10 01:32:53.869602641 -0300
@@ -307,7 +307,7 @@
 CROSS_COMPILE = /opt/montavista/pro/devkit/arm/v5t_le_mvl5/bin/arm_v5t_le-
 endif
 
-export OSABL RT28xx_DIR RT28xx_MODE LINUX_SRC CROSS_COMPILE CROSS_COMPILE_INCLUDE PLATFORM RELEASE CHIPSET MODULE RTMP_SRC_DIR LINUX_SRC_MODULE TARGET HAS_WOW_SUPPORT
+export OSABL RT28xx_DIR RT28xx_MODE LINUX_SRC CROSS_COMPILE CROSS_COMPILE_INCLUDE PLATFORM RELEASE CHIPSET MODULE RTMP_SRC_DIR LINUX_SRC_MODULE TARGET HAS_WOW_SUPPORT DESTDIR
 
 # The targets that may be used.
 PHONY += all build_tools test UCOS THREADX LINUX release prerelease clean uninstall install libwapi osabl
diff -Naur rt3290sta.orig/os/linux/Makefile.6 rt3290sta/os/linux/Makefile.6
--- rt3290sta.orig/os/linux/Makefile.6	2015-09-09 18:33:56.568310269 -0300
+++ rt3290sta/os/linux/Makefile.6	2015-09-10 01:58:11.375659233 -0300
@@ -255,13 +255,13 @@
 endif
 
 install:
-	rm -rf $(DAT_PATH)
-	$(shell [ ! -f /etc/Wireless ] && mkdir /etc/Wireless)
-	mkdir $(DAT_PATH)
-	cp $(RT28xx_DIR)/$(DAT_FILE_NAME) $(DAT_PATH)/.
-	install -d $(LINUX_SRC_MODULE)
-	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(LINUX_SRC_MODULE)
+	install -d $(DESTDIR)/$(DAT_PATH)
+	install -m 644 -c $(RT28xx_DIR)/$(DAT_FILE_NAME) $(DESTDIR)/$(DAT_PATH)/.
+	install -d $(DESTDIR)/$(LINUX_SRC_MODULE)
+	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(DESTDIR)$(LINUX_SRC_MODULE)
+ifndef DESTDIR
 	/sbin/depmod -a ${KERN_VER}
+endif
 
 uninstall:
 #	rm -rf $(DAT_PATH)
